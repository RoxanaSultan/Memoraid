<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF"/> <!-- Background color -->
    <stroke
        android:width="3dp"
        android:color="@color/light_green"/> <!-- Border color and width -->
    <corners android:radius="8dp"/> <!-- Rounded corners (optional) -->
    <padding
        android:left="10dp"
        android:right="10dp"
        android:top="10dp"
        android:bottom="10dp"/>
</shape>
